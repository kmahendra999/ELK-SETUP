---
title: 12 Setting Up Elastic Agent
description: 
published: true
date: 2026-03-03T12:18:20.686Z
tags: 
editor: ckeditor
dateCreated: 2024-08-09T18:05:42.523Z
---

`add this in /etc/hosts`

`10.50.14.82 elk01.elkdomainname.com`  
`10.50.14.110 elk02.elkdomainname.com`  
`10.50.14.239 elk03.elkdomainname.com`  
`90.142.214253 elk04.elkdomainname.com`

Make a folder anywhere.

Keep two files in that. `ca_bundle.crt & linux-installation-script.sh`

*(First ca.crt we had while generated certificate before, copy that and rename that as ca\_bundle.crt)*

```
ubuntu@mh-us-node001:~/Linux-Prod-Monitor$ cat ca_bundle.crt 
-----BEGIN CERTIFICATE-----

...  (about 20 lines like these in your real ca.crt)  ...
RFVNTVkgQ0VSVElGSUNBVEUgLSBSRVBMQUNFIFdJVEggWU9VUiBPV04gY2EuY3J0
-----END CERTIFICATE-----
```

Do not copy the dummy certificate above. Paste the actual base64 body of **your** cluster's `ca.crt` there — roughly 20 lines of 64 characters each, looking like `MIIDSjCCAjKgAwIBAgIVAMLA6Urb...`, exactly as it appears in the `ca.crt` produced when you generated the certificates.

Second in the same folder create a file `linux-installation-script.sh or any other desired name.`

Replace the dummy token in the script below with the enrollment token you copied from the Linux Servers policy in the previous step. It is a base64 string ending in `==`, for example `RXhhbXBsZUVucm9sbG1lbnRUb2tlbjpSRVBMQUNFLU1F==`. This token lets any host join your Fleet, so treat it as a password and never commit it.

```
ubuntu@mh-us-node001:~/Linux-Prod-Monitor$ cat linux-installation-script.sh 
#!/bin/bash

# Define variables for fleet_url and token
fleet_url="https://elk01.elkdomainname.com:8220"
token="RXhhbXBsZUVucm9sbG1lbnRUb2tlbjpSRVBMQUNFLU1F=="
certificate="ca_bundle.crt"
# Function to check telnet connectivity
function check_telnet {
  local host="$1"
  local port="$2"
  nc -z "$host" "$port" &> /dev/null
}

# Verify telnet connectivity to the specified domains
if check_telnet "elk01.elkdomainname.com" "8220" && check_telnet "elk01.elkdomainname.com" "9200"; then
  echo "Telnet connectivity to domains is successful."
else
  echo "Telnet connectivity to one or both domains failed. Exiting."
  exit 1
fi

# Download and install Elastic Agent
echo "Downloading Agent..."
curl -L -O https://artifacts.elastic.co/downloads/beats/elastic-agent/elastic-agent-8.14.2-linux-x86_64.tar.gz
echo "Extracting Agent..."
tar xzvf elastic-agent-8.14.2-linux-x86_64.tar.gz
cp $certificate elastic-agent-8.14.2-linux-x86_64
cd elastic-agent-8.14.2-linux-x86_64
echo "Installing Agent..."
# Get the current working directory and store it in a variable
current_path=$(pwd)
sudo ./elastic-agent install --url="$fleet_url" --enrollment-token="$token" --certificate-authorities="$current_path/$certificate"
```

Now Copy these both files in a folder.

Copy folder to a machine which machine you want to monitor using elk stack.

Run the Linux script using root user.

*(root user recommended – so it can collect the logs properly.)*

*Also, If on node there is services running you can turn off manually or by script.*

`sudo systemctl stop logstash.service`  
`sudo systemctl disable logstash.service`   
`sudo systemctl stop metricbeat.service`  
`sudo systemctl disable metricbeat.service`  
`sudo systemctl disable filebeat.service`  
`sudo systemctl stop filebeat.service`
