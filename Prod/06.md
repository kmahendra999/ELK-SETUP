---
title: 06 Setting  up Kibana
description: 
published: true
date: 2026-03-03T12:18:05.696Z
tags: 
editor: ckeditor
dateCreated: 2024-08-09T18:02:41.486Z
---

**Setting up the kibana system user password :**  
`/usr/share/elasticsearch/bin/elasticsearch-reset-password -u kibana_system`

**Generate Kibana Encryption Keys:**

`/usr/share/kibana/bin/kibana-encryption-keys genrate`

*last 3 lines output of this command in setting section we will use in kibana.yml file*

`systemctl enable --now kibana`

`setcap ‘capnetbind_service=+ep’ /usr/share/kibana/node/bin/node`

Enter Command:

`crontab -e`

Enter the below given content in the file and save

`@reboot setcap 'cap_net_bind_service=+ep' /usr/share/kibana/node/bin/node`

> `This above steps are recommended also we need to do if kibana is not running.`

`systemctl status kibana`

`systemctl restart kibana`

`systemctl status kibana`
